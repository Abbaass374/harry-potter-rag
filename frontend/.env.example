# ---------------------------------------------------------------------------
# Frontend configuration. Copy to `.env`:  cp .env.example .env
# ---------------------------------------------------------------------------

# Base URL of the FastAPI backend (no trailing slash needed).
API_BASE_URL=http://localhost:8000

# Client-side timeouts (seconds). Generation can be slow on CPU.
QUERY_TIMEOUT=120
HEALTH_TIMEOUT=5
