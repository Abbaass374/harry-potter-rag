# Copy the contents into Streamlit Cloud: Manage app -> Settings -> Secrets.
# (For LOCAL `streamlit run streamlit_app.py`, either create
#  .streamlit/secrets.toml with these values, or set them in backend/.env.)

GROQ_API_KEY = "gsk_your_key_here"
GROQ_MODEL = "openai/gpt-oss-20b"
